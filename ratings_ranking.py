from collections import Counter
import string
if:
    n=int(input())
    num=[int(i) for i in input().split()]
    ns=set(num)
    count=0
    num.sort()
    count+=num[0]-1
    for i in range(n):
        
        
    #print(var)
