#include <bits/stdc++.h>
#include <cmath>
using namespace std;
#define ll long long int
ll sumt=0;
#define space " "
#define array vector<ll>
#define array2 vector< vector<ll> > 
#define fr(i,l,n) for(int i=l;i<n;i++) 
#define backward(i,l,n) for(int i=l;i>l;i--)
#define fast ios::sync_with_stdio(0); cin.tie(0); cout.tie(0)
#define all(c) (c).begin(),(c).end()
#define len(v) (int)(v).size()
#define sort(num) sort(all(num))
#define reverse(num,n) for(i,0,n/2){ll z;z=num[i];num[i]=num[n-i-1];num[n-i-1]=z;}
ll count=0;
void find(ll l,ll r,array* arr){
    if(len(arr)==1||l-r+1==1){
        return;
    }
    array nw;

}
ll solve()
{
  ll n,x,a,b,c,d,q,k=0,j=0,m,flag=0,i=0,ans=0,l,r;
  sumt=0;
  cin>>n;
  array num(n);
  fr(i,0,n){
      cin>>num[i];
  }
  
}
int main()
{
    fast;
     ll t = 1;
    //  cin >> t;
     while(t--)
     {
          solve();
     }
     return 0;
}